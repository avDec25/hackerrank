#include <bits/stdc++.h>
//#include "prettyprint.h"
using namespace std;
//using namespace std::chrono;
using ll = long long;
#define INF (numeric_limits<int>::max())

string reverseShuffleMerge(string s) {
	
}

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    string x; cin >> x;
    cout << reverseShuffleMerge(x);

    return 0;
}